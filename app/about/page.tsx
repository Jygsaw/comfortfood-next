import React from 'react';
import { type Metadata } from 'next';

export const metadata: Metadata = {
    title: 'About Us | Vizro',
};

const Page = () => (
    <>
        This is the about page.
    </>
);

export default Page;
