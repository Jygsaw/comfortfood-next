import React from 'react';
import { type Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Vizro',
};

const Page = () => (
    <>
        Hello, Homepage.
    </>
);

export default Page;
